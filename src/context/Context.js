import { createContext } from "react";
export const myContext =createContext({

});
export const themes = {
    light: {
      foreground: "#000000",
      background: "#eeeeee",
      color: "#000000"
    },
    dark: {
      foreground: "#ffffff",
      background: "#222222",
      color: "#ffffff"
    }
  };